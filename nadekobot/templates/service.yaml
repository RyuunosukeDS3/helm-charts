apiVersion: v1
kind: Service
metadata:
  name: {{ include "nadekobot.fullname" . }}
  labels:
    {{- include "nadekobot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nadekobot.selectorLabels" . | nindent 4 }}
