apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nadekobot.fullname" . }}-test-connection"
  labels:
    {{- include "nadekobot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nadekobot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
